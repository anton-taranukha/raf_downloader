name: Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}:latest

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}

      - name: Copy server compose
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: docker-compose.server.yml
          target: /home/ubuntu/raf

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            set -e
            cd /home/ubuntu/raf
            mkdir -p secrets
            test -f secrets/service-account.json
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
            docker compose -f docker-compose.server.yml pull
            docker compose -f docker-compose.server.yml up -d
            for attempt in $(seq 1 30); do
              if curl -fsS http://127.0.0.1:8080/ >/tmp/raf-index.html; then
                break
              fi
              sleep 2
            done
            asset_path=$(sed -n 's/.*src="\([^"]*\.js\)".*/\1/p' /tmp/raf-index.html)
            test -n "$asset_path"
            curl -fsS "http://127.0.0.1:8080${asset_path}" | grep -q "Дорожні"
            docker image prune -f
